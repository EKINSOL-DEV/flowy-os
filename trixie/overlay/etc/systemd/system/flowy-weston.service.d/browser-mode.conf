[Service]
ExecStart=
ExecStart=/usr/bin/flowy-weston-launch
# labwc has no sd_notify; run both compositors as simple services. Readiness
# gating isn't needed (flowy-kiosk polls nginx/API itself) and crash recovery
# comes from Restart=on-failure.
Type=simple
WatchdogSec=0
NotifyAccess=all
